--
title: Broken Frontmatter
description: The dashes above are malformed (only two).
---

# Frontmatter Test
This file triggers **Z506 (Malformed Frontmatter)** because the opening delimiter is `--` instead of `---`. It must be on the very first line to trigger.